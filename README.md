# Summary Toolkit

A modular project summary generator. Produces a structured summary
report from project source files.

## Quick Start

```bash
make setup
```

This installs dependencies and generates a sample summary report in
`build/summary.txt`.
